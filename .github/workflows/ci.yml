name: "CI"

on:
  push:
    paths: ~
  workflow_dispatch: ~

jobs:
  hello:
    runs-on: "ubuntu-latest"
    steps:
      - name: "Hello world"
        run: echo "Hello world"
      - name: "Multiple Lines"
        run: |
          echo "Say"
          exit 1
          echo "Hello"
          echo "To"
          echo "You"
        continue-on-error: true
      - name: "List directory"
        run: ls
