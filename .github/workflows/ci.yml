name: "CI"

on:
  push:
    paths: ~

jobs:
  hello:
    runs-on: "ubuntu-latest"
    steps:
      - name: "Hello world"
        run: echo "Hello world"
      - name: "Multiple Lines"
        run: |
          echo "Say"
          echo "Hello"
          echo "To"
          echo "You"
      - name: "List directory"
        run: ls
