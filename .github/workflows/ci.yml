name: "CI"

on:
  push:
    paths: ~
  workflow_dispatch: ~

jobs:
  hello:
    runs-on: "ubuntu-latest"
    continue-on-error: true
    steps:
      - name: "Hello world"
        run: echo "Hello world"
      - name: "Multiple Lines"
        run: |
          echo "Say"
          exit 1
          echo "Hello"
          echo "To"
          echo "You"
      - name: "List directory"
        run: ls

  hello2:
    runs-on: "ubuntu-lastest"
    steps:
      - name: "Coucou"
        run: "echo Coucou"
